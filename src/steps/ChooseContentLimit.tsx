import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const ChooseContentLimit: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from ChooseContentLimit</h1>
    </div>
  );
};

export default ChooseContentLimit;
