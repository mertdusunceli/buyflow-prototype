import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const ChoosePaymentOption: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from ChoosePaymentOption</h1>
    </div>
  );
};

export default ChoosePaymentOption;
