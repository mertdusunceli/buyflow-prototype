import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const RequireInsurance: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from RequireInsurance</h1>
    </div>
  );
};

export default RequireInsurance;
