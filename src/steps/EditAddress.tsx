import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const EditAddress: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from EditAddress</h1>
    </div>
  );
};

export default EditAddress;
