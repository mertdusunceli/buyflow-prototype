import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const AddRoommates: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from AddRoommates</h1>
    </div>
  );
};

export default AddRoommates;
