import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const PurchaseOptions: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from PurchaseOptions</h1>
    </div>
  );
};

export default PurchaseOptions;
