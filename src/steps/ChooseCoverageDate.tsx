import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const ChooseCoverageDate: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from ChooseCoverageDate</h1>
    </div>
  );
};

export default ChooseCoverageDate;
