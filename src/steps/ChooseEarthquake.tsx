import { FC, ChangeEvent } from "react";
import { StepComponentProps } from "../App";

const ChooseEarthquake: FC<StepComponentProps> = ({ onChange }) => {
  return (
    <div>
      <h1>Hello world from ChooseEarthquake</h1>
    </div>
  );
};

export default ChooseEarthquake;
